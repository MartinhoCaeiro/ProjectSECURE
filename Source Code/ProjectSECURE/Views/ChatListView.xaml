<Window x:Class="ProjectSECURE.Views.ChatListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ProjectSECURE"
        Height="600"
        Width="400"
        WindowStyle="SingleBorderWindow"
        SizeToContent="Manual"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundBrush}">
    <!-- Custom scrollbar style for chat list -->
    <Window.Resources>
        <Style x:Key="CustomScrollBar"
               TargetType="ScrollBar">
            <Setter Property="Width"
                    Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{DynamicResource ControlBackgroundBrush}">
                            <Track Name="PART_Track"
                                   IsDirectionReversed="true"
                                   Value="{TemplateBinding Value}"
                                   Minimum="{TemplateBinding Minimum}"
                                   Maximum="{TemplateBinding Maximum}"
                                   ViewportSize="{TemplateBinding ViewportSize}">
                                <Track.Thumb>
                                    <Thumb Background="{DynamicResource ButtonBackgroundBrush}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <!-- Layout: Header, chat list, new chat button -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Chat list -->
            <RowDefinition Height="Auto"/>
            <!-- New chat button -->
        </Grid.RowDefinitions>

        <!-- Header section with title, settings, and logout buttons -->
        <DockPanel Grid.Row="0"
                   Margin="10"
                   LastChildFill="False">
            <TextBlock Text="Conversas"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="{DynamicResource ForegroundBrush}"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left"/>
            <Button Content="⚙"
                    Width="36"
                    Height="32"
                    Click="OpenSettings"
                    DockPanel.Dock="Right"/>

            <Button Content="↩"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    DockPanel.Dock="Right"
                    Width="36"
                    Height="32"
                    Margin="10"
                    Click="Logout_Click"/>

        </DockPanel>

        <!-- List of chats for the current user with custom ScrollViewer -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Chats}"
                 SelectedItem="{Binding SelectedChat}"
                 MouseDoubleClick="Chat_DoubleClick"
                 Background="Transparent"
                 BorderThickness="0"
                 Margin="10">
            <ListBox.Resources>
                <Style TargetType="ScrollBar"
                       BasedOn="{StaticResource CustomScrollBar}"/>
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="#22FFFFFF"
                            CornerRadius="6"
                            Padding="12,8"
                            Margin="0,4">
                        <!-- Remove lateral margin -->
                        <TextBlock Text="{Binding Name}"
                                   FontSize="16"
                                   Foreground="{DynamicResource ForegroundBrush}"/>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment"
                            Value="Stretch"/>
                    <Setter Property="Padding"
                            Value="0"/>
                    <!-- Remove item padding -->
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Button to create a new chat -->
        <Button Grid.Row="2"
                Content="➕ Nova Conversa"
                Command="{Binding NewChatCommand}"
                HorizontalAlignment="Center"
                Margin="10"
                Width="200">
            <Button.Style>
                <Style TargetType="Button"
                        BasedOn="{StaticResource {x:Type Button}}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsWireGuardActive}"
                                Value="False">
                            <Setter Property="Visibility"
                                    Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
